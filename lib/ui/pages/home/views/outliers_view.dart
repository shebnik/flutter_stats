import 'package:flutter/material.dart';

class OutliersView extends StatelessWidget {
  const OutliersView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'OutliersView is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
