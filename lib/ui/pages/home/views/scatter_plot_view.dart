import 'package:flutter/material.dart';

class ScatterPlotView extends StatelessWidget {
  const ScatterPlotView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'ScatterPlotView is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
